{
  "name": "opencode-adapter",
  "version": "1.1.0",
  "description": "Local Anthropic-compatible adapter for OpenCode Go",
  "type": "module",
  "bin": {
    "opencode-adapter": "dist/src/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "files": [
    "dist",
    "README.md",
    "README.zh-CN.md",
    "LICENSE"
  ],
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
