{
  "name": "@tanzz/agentx",
  "version": "1.0.0",
  "description": "Local Anthropic-compatible adapter for OpenCode",
  "license": "MIT",
  "type": "module",
  "bin": {
    "agentx": "dist/src/cli.js"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && cd dist && node --test"
  },
  "files": [
    "dist/src",
    "README.md",
    "README.zh-CN.md",
    "LICENSE"
  ],
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@clack/prompts": "^1.7.0"
  }
}
