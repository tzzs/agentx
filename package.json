{
  "name": "@tanzz/agentx",
  "version": "3.4.0",
  "description": "Local Anthropic-compatible adapter for OpenCode",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tzzs/agentx.git"
  },
  "license": "MIT",
  "type": "module",
  "bin": {
    "agentx": "dist/src/cli.js"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "npm run build && cd dist && node --test",
    "test:one": "npm run build && node --test"
  },
  "files": [
    "dist/src",
    "README.md",
    "README.zh-CN.md",
    "LICENSE"
  ],
  "devDependencies": {
    "@types/node": "^22.0.0",
    "eslint": "^10.11.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.70.0"
  },
  "dependencies": {
    "@clack/prompts": "^1.7.0"
  }
}
